{
  "name": "flowsight",
  "version": "1.0.0",
  "description": "Sistema IoT completo para monitoreo de alarmas y control de válvulas",
  "private": true,
  "workspaces": [
    "backend",
    "web-admin",
    "mqtt-broker"
  ],
  "scripts": {
    "backend": "cd backend && npm run dev",
    "web": "cd web-admin && npm run dev",
    "mqtt": "cd mqtt-broker && npm run dev",
    "mobile": "cd mobile && npx expo start",
    "install:all": "npm install && cd mobile && npm install",
    "dev": "concurrently \"npm run backend\" \"npm run mqtt\" \"npm run web\"",
    "db:setup": "cd database && mysql -u root -p < init.sql"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "author": "FlowSight Team",
  "license": "MIT"
}

